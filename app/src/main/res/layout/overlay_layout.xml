<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:gravity="end"
    android:orientation="vertical"
    android:padding="4dp">

    <TextView
        android:id="@+id/overlay_status_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_status_banner"
        android:elevation="6dp"
        android:maxWidth="260dp"
        android:paddingStart="14dp"
        android:paddingTop="9dp"
        android:paddingEnd="14dp"
        android:paddingBottom="9dp"
        android:textColor="#FFFFFFFF"
        android:textSize="15sp"
        android:textStyle="bold"
        android:visibility="gone" />

    <TextView
        android:id="@+id/overlay_replay_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="7dp"
        android:background="@drawable/overlay_replay_background"
        android:clickable="true"
        android:elevation="4dp"
        android:focusable="true"
        android:paddingStart="13dp"
        android:paddingTop="8dp"
        android:paddingEnd="13dp"
        android:paddingBottom="8dp"
        android:text="@string/replay_last_guidance"
        android:textColor="#005F45"
        android:textSize="14sp"
        android:visibility="gone" />

    <ImageView
        android:id="@+id/overlay_button"
        android:layout_width="58dp"
        android:layout_height="58dp"
        android:layout_marginTop="8dp"
        android:background="@drawable/bg_overlay_button"
        android:contentDescription="@string/app_name"
        android:elevation="10dp"
        android:padding="14dp"
        android:src="@drawable/ic_assistant" />
</LinearLayout>
