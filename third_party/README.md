# Native dependencies

`llama.cpp-omni` is the native runtime used by the official OpenBMB MiniCPM-V Android demo.

The required pinned commit is:

```text
da241979b95b13e747f7c3f8d6821930e0263d33
```

Initialize the submodule before building the MiniCPM-V runtime.
